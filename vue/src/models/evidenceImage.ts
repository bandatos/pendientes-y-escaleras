/* Para el endpoint de cada imagen. */
export default class evidenceImage {
    stair_id = 0; //in headers
    File = new File();
   

    constructor(item) {
        if (item) {

        } else {

        }
    }
}