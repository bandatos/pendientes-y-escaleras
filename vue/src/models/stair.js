export default class stair {
    id = 0; //Identificador que se carga al inicio, Y se asginan sus id's
    station_id = 0; //Información de identificador.
    code_identifiers = [];
    start_position = "";
    end_position = "";
    description = "";
    is_working = true;
    /* evidence_images = [  //readonly
        "url",
        "url",
    ]; */ 

    constructor(item) {
        if (item) {

        } else {

        }
    }
}